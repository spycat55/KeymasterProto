{
  "name": "keymaster_proto-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @spycat55/keymaster_proto && npm run build -w @spycat55/keymaster_utils"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "ts-proto": "^2.7.5"
  }
}
